type Query {
  getUsers: [User]
  user(id: ID!): User
}

type Mutation {
  createUser(name: String, specialty:String): User
}

type User {
  id: Int
  name: String
  specialty: String
  email: String
}